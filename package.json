{
  "name": "n-back-math",
  "version": "1.0.0",
  "private": true,
  "description": "Mobile-first arithmetic n-back trainer.",
  "scripts": {
    "build": "npm run build:web",
    "build:web": "node scripts/build-web.js",
    "generate:icon": "scripts/generate-icon.sh",
    "generate:screenshots": "scripts/generate-app-store-screenshots.sh",
    "open:ios": "open ios/App/App.xcodeproj"
  }
}
